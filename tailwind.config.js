/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [],
  theme: {
    extend: {
        colors: {
        primary: 'rgba(255, 255, 255, 0.25)',
        primaryWhite: '#ffffff', // Este será el color que usaremos cuando hagas scroll
      },
    },
  },
  plugins: [],
}

